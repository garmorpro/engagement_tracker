# engagement_tracker
## Dependencies

This application is built using:
- Custom PHP
- Vanilla JavaScript
- Plain CSS

No third-party libraries or package managers are used.
As a result, no dependency lock files (Composer, npm, etc.) are present.
